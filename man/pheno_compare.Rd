% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluation.R
\name{pheno_compare}
\alias{pheno_compare}
\title{Compare Anomaly Phenotypes Across Two Time Series}
\usage{
pheno_compare(
  result_a,
  result_b,
  method = c("bhattacharyya", "cosine", "euclidean")
)
}
\arguments{
\item{result_a}{An \code{AnomPhenoResult} from series A.}

\item{result_b}{An \code{AnomPhenoResult} from series B.}

\item{method}{Character. Similarity metric:
\describe{
\item{\code{"bhattacharyya"}}{Bhattacharyya coefficient (default). Robust
to different sample sizes.}
\item{\code{"cosine"}}{Cosine similarity of phenotype centroid vectors.}
\item{\code{"euclidean"}}{Negative Euclidean distance (closer = more similar).}
}}
}
\value{
A list of class \code{"AnomPhenoComparison"} with:
\describe{
\item{\code{similarity_matrix}}{Numeric matrix (n_pheno_A x n_pheno_B).}
\item{\code{best_matches}}{\code{data.frame}: best-matching phenotype pair for
each phenotype in A.}
\item{\code{overall_similarity}}{Single numeric: mean of best-match scores.}
\item{\code{method}}{Method used.}
}
}
\description{
Quantifies the \strong{structural similarity} between phenotype families discovered
in two different time series (e.g., two sensors, before-and-after treatment,
two patients). Each phenotype from series A is compared to each phenotype
from series B using the \strong{Bhattacharyya coefficient} of their feature
distributions in the standardised 27-dimensional space.

The output similarity matrix has a natural interpretation: a value close to 1
means the two phenotypes have nearly identical feature distributions; a value
near 0 means they are morphologically distinct.
}
\examples{
set.seed(1); x_a <- sin(seq_len(300) / 5) + rnorm(300, sd = 0.1)
x_a[50] <- 7; x_a[150:155] <- x_a[150:155] + 3
set.seed(2); x_b <- cos(seq_len(300) / 5) + rnorm(300, sd = 0.1)
x_b[80] <- 6; x_b[200:205] <- x_b[200:205] + 2.5
ra <- suppressMessages(pheno_diagnose(x_a, q = 2.5))
rb <- suppressMessages(pheno_diagnose(x_b, q = 2.5))
if (!is.null(ra) && !is.null(rb)) {
  cmp <- pheno_compare(ra, rb)
  print(cmp)
}

}
