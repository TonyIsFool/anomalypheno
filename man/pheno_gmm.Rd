% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{pheno_gmm}
\alias{pheno_gmm}
\title{Gaussian Mixture Model Phenotyping with Automatic Model Selection}
\usage{
pheno_gmm(
  result,
  k_max = 6L,
  n_init = 5L,
  max_iter = 150L,
  tol = 1e-06,
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{result}{An \code{AnomPhenoResult} from \code{\link[=pheno_diagnose]{pheno_diagnose()}}.}

\item{k_max}{Integer. Maximum number of mixture components. Default: \code{6L}.}

\item{n_init}{Integer. Random initialisations per k. Default: \code{5L}.}

\item{max_iter}{Integer. Maximum EM iterations per fit. Default: \code{150L}.}

\item{tol}{Numeric. Log-likelihood convergence tolerance. Default: \code{1e-6}.}

\item{seed}{Integer. Random seed for reproducibility.}

\item{verbose}{Logical. Print BIC table. Default: \code{TRUE}.}
}
\value{
An \code{AnomPhenoGMM} object with fields:
\describe{
\item{\code{labels}}{Hard phenotype assignments (MAP estimate).}
\item{\code{posterior}}{n \eqn{\times} K soft probability matrix.}
\item{\code{mu}}{Component mean vectors (K \eqn{\times} p).}
\item{\code{best_k}}{Selected number of components.}
\item{\code{bic_per_k}}{BIC value for each k in 1..k_max.}
}
}
\description{
An alternative to the Mapper-based phenotyping engine that fits a
\strong{Gaussian Mixture Model} (diagonal covariance) to the standardised
27-dimensional feature space via the EM algorithm. The number of
components K is chosen automatically using the \strong{Bayesian Information
Criterion} (BIC), balancing goodness-of-fit against model complexity.

Unlike the hard Mapper assignments, GMM returns \strong{soft posterior
probabilities} P(phenotype k | instance i), enabling uncertainty-aware
downstream analysis.
}
\examples{
data(anomaly_benchmark)
result <- suppressMessages(pheno_diagnose(anomaly_benchmark$x, q = 2.5))
if (!is.null(result) && nrow(result$instance_table) >= 4) {
  gmm <- suppressMessages(pheno_gmm(result, k_max = 4L, n_init = 2L, seed = 42))
  print(gmm)
}

}
