% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{pheno_track}
\alias{pheno_track}
\title{Track Phenotype Prevalence Over Sliding Windows}
\usage{
pheno_track(result, window_size = 100L, step = 20L)
}
\arguments{
\item{result}{An \code{AnomPhenoResult} from \code{\link[=pheno_diagnose]{pheno_diagnose()}} or \code{pheno_prune()}.}

\item{window_size}{Integer. Window size in time steps. Default: \code{100L}.}

\item{step}{Integer. Step size between windows. Default: \code{20L}.}
}
\value{
An \code{AnomPhenoTrack} object with:
\describe{
\item{\code{track_matrix}}{Numeric matrix (\eqn{W \times K}): phenotype
prevalence proportions per window.}
\item{\code{window_starts}}{Integer vector of window start indices.}
\item{\code{window_midpoints}}{Numeric vector of window midpoints.}
\item{\code{n_phenotypes}}{Number of phenotypes.}
}
}
\description{
Computes the \strong{fractional prevalence of each phenotype} within overlapping
windows of the time series, revealing how the distribution of anomaly
types evolves over time.

This is particularly useful for:
\itemize{
\item Detecting \strong{regime shifts} -- sustained changes in which phenotype
dominates.
\item Monitoring \strong{treatment effects} -- e.g., does the spike phenotype
diminish after a clinical intervention?
\item \strong{Longitudinal studies} -- comparing phenotype distributions across
temporal segments.
}
}
\examples{
data(anomaly_benchmark)
result <- suppressMessages(pheno_diagnose(anomaly_benchmark$x, q = 2.5))
if (!is.null(result)) {
  trk <- pheno_track(result, window_size = 120L, step = 20L)
  plot(trk)
}

}
