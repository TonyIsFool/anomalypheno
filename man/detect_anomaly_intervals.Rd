% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{detect_anomaly_intervals}
\alias{detect_anomaly_intervals}
\title{Detect Anomaly Interval Start Indices}
\usage{
detect_anomaly_intervals(
  x,
  q = 3,
  gap = 3L,
  engine = c("sr", "lof"),
  lof_k = 5L,
  window = 5L
)
}
\arguments{
\item{x}{Numeric vector. The time series.}

\item{q}{Numeric. Threshold. For \code{engine = "sr"}: q-sigma multiplier
(default 3.0). For \code{engine = "lof"}: LOF score cutoff (default 2.0).}

\item{gap}{Integer. Minimum gap (in steps) to separate distinct anomaly
intervals after raw flagging. Default: 3.}

\item{engine}{Character. \code{"sr"} (Spectral Residual, default) or \code{"lof"}
(Local Outlier Factor sliding window).}

\item{lof_k}{Integer. Number of nearest neighbours for LOF. Default: 5.}

\item{window}{Integer. Window size for LOF sliding-window summary. Default: 5.}
}
\value{
Integer vector of anomaly interval start indices (1-based).
}
\description{
Identifies anomalous intervals in a time series using either the
\strong{Spectral Residual} (SR) engine (Ren et al., 2019) or a \strong{Local Outlier
Factor} (LOF) sliding-window engine. The LOF engine is more robust to
slowly-drifting baselines; SR is faster and better at sharp point spikes.
}
\references{
Ren, H., et al. (2019). Time-series anomaly detection service at Microsoft.
\emph{KDD '19}. \doi{10.1145/3292500.3330680}
}
