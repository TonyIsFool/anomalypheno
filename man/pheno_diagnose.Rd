% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anomalypheno.R
\name{pheno_diagnose}
\alias{pheno_diagnose}
\title{Diagnose Anomaly Phenotypes in a Time Series}
\usage{
pheno_diagnose(
  x,
  q = 3,
  engine = c("sr", "lof"),
  seg_len = 10L,
  gap = 3L,
  n_intervals = NULL,
  overlap = 0.5
)
}
\arguments{
\item{x}{Numeric vector. The input time series.}

\item{q}{Numeric. Detection threshold. SR mode: q-sigma multiplier
(typical: 2.5-3.5). LOF mode: LOF score cutoff (typical: 1.5-3.0).
Default: 3.0.}

\item{engine}{Character. Detection engine: \code{"sr"} (Spectral Residual,
default) or \code{"lof"} (Local Outlier Factor sliding window).}

\item{seg_len}{Integer. Length of each anomaly segment window. Default: 10.}

\item{gap}{Integer. Minimum index gap between distinct anomaly intervals.
Default: 3.}

\item{n_intervals}{Integer or \code{NULL}. Base Mapper cover intervals. If \code{NULL}
(default), auto-selected via Sturges rule.}

\item{overlap}{Numeric. Mapper interval overlap fraction. Default: 0.5.}
}
\value{
An \code{AnomPhenoResult} object, or \code{NULL} if no anomalies are detected.
Print, summary, and plot methods are available.
}
\description{
The single entry-point for the \strong{AnomalyPheno} pipeline. Orchestrates four
layers automatically:
\enumerate{
\item \strong{Detection} -- flags anomaly intervals using the Spectral Residual (SR)
or Local Outlier Factor (LOF) engine.
\item \strong{Feature extraction} -- computes a \strong{27-dimensional} signature per
anomaly covering morphological, spectral, context, severity, and
complexity domains.
\item \strong{Multi-Resolution Mapper} -- runs topological phenotyping at three
resolution scales and combines assignments by majority vote, producing
a \strong{stability score} for each instance.
\item \strong{Characterisation} -- assigns auto-names, canonical templates, severity
classes, phenotype confidence scores, and a feature importance ranking.
}
}
\examples{
data(anomaly_benchmark)
result <- pheno_diagnose(anomaly_benchmark$x, q = 2.5)
print(result)
plot(result)                          # canonical template gallery
plot(result, type = "timeline")      # phenotype-coloured series
plot(result, type = "profile")       # feature discriminability chart
plot(result, type = "landscape")     # Mapper topological graph

# Classify a new anomaly
pred <- pheno_predict(result, new_segment = c(0.1, 7.0, 0.2))

}
\seealso{
\code{\link[=detect_anomaly_intervals]{detect_anomaly_intervals()}}, \code{\link[=extract_anomaly_features]{extract_anomaly_features()}},
\code{\link[=mapper_phenotype]{mapper_phenotype()}}, \code{\link[=characterise_phenotypes]{characterise_phenotypes()}}, \code{\link[=pheno_predict]{pheno_predict()}},
\code{\link[=plot_phenotype_timeline]{plot_phenotype_timeline()}}, \code{\link[=plot_phenotype_gallery]{plot_phenotype_gallery()}},
\code{\link[=plot_phenotype_profile]{plot_phenotype_profile()}}, \code{\link[=plot_phenotype_landscape]{plot_phenotype_landscape()}}
}
