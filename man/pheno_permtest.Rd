% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{pheno_permtest}
\alias{pheno_permtest}
\title{Permutation Test for Phenotype Cluster Structure}
\usage{
pheno_permtest(result, B = 999L, seed = 42L, verbose = TRUE)
}
\arguments{
\item{result}{An \code{AnomPhenoResult} from \code{\link[=pheno_diagnose]{pheno_diagnose()}}.}

\item{B}{Integer. Number of permutations. Default: \code{999L}.}

\item{seed}{Integer. Random seed. Default: \code{42L}.}

\item{verbose}{Logical. Print progress. Default: \code{TRUE}.}
}
\value{
An \code{AnomPhenoPermTest} object with fields \code{observed_silhouette},
\code{permuted_silhouettes}, \code{p_value}, and \code{significant} (p < 0.05).
}
\description{
Tests the null hypothesis H\eqn{_0}: anomaly instances are exchangeable
across phenotypes (i.e., the observed phenotype structure could arise by
chance).

Under H\eqn{_0}, phenotype labels are randomly permuted \code{B} times and the
\strong{mean silhouette width} is recomputed for each permutation. The
empirical p-value is:
\deqn{p = \frac{1 + |\{b : S_b \geq S_{\text{obs}}\}|}{1 + B}}

A p-value < 0.05 provides statistical evidence that the phenotype
partition captures genuine structure in the feature space.
}
\examples{
data(anomaly_benchmark)
result <- suppressMessages(pheno_diagnose(anomaly_benchmark$x, q = 2.5))
if (!is.null(result) && result$mapper$n_phenotypes >= 2) {
  pt <- suppressMessages(pheno_permtest(result, B = 99L, seed = 1L))
  print(pt)
}

}
