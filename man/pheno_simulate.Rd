% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{pheno_simulate}
\alias{pheno_simulate}
\title{Simulate Time Series with Controlled Anomaly Phenotypes}
\usage{
pheno_simulate(
  n = 600L,
  phenotype_defs = NULL,
  noise_sd = 0.1,
  baseline_freq = 30,
  min_gap = 20L,
  seed = 42L
)
}
\arguments{
\item{n}{Integer. Total series length. Default: 600.}

\item{phenotype_defs}{List of phenotype definition lists. Each element may
contain: \code{type} (character), \code{amplitude} (numeric), \code{duration} (integer),
\code{n_instances} (integer), \code{frequency} (numeric, for \code{"oscillation"} type).
If \code{NULL}, uses a canonical 3-phenotype benchmark definition.}

\item{noise_sd}{Numeric. Standard deviation of Gaussian baseline noise.
Default: 0.1.}

\item{baseline_freq}{Numeric. Period (in steps) of the sinusoidal baseline.
Default: 30.}

\item{min_gap}{Integer. Minimum spacing between injected anomaly windows.
Default: 20.}

\item{seed}{Integer. Random seed for reproducibility. Default: 42.}
}
\value{
A list with:
\describe{
\item{\code{x}}{Numeric vector: the simulated time series.}
\item{\code{true_labels}}{Integer vector (length \code{n}): phenotype ID at each
time step (0 = normal).}
\item{\code{anomaly_idx}}{Integer vector: start indices of injected anomalies.}
\item{\code{true_phenotype_id}}{Integer vector: phenotype ID per anomaly
instance (parallel to \code{anomaly_idx}).}
\item{\code{phenotype_defs}}{The phenotype definitions used.}
\item{\code{seed}}{The seed used.}
}
}
\description{
Generates synthetic univariate time series with \strong{known, controlled anomaly
phenotypes} injected at non-overlapping positions. The function is designed
for three primary use cases:

\enumerate{
\item \strong{Benchmarking} -- generate series where ground truth is known, then
evaluate how accurately \code{\link[=pheno_diagnose]{pheno_diagnose()}} recovers the structure.
\item \strong{Power analysis} -- vary amplitude, duration, or noise to find the
detection boundary for each phenotype type.
\item \strong{Reproducible research} -- specify \code{seed} to ensure exact
replicability across platforms.
}
\subsection{Phenotype Types}{

\describe{
\item{\code{"spike"}}{Gaussian-shaped sharp peak (high excess kurtosis, short).}
\item{\code{"depression"}}{Gaussian-shaped trough (negative spike).}
\item{\code{"plateau"}}{Sustained level shift (low oscillation, long duration).}
\item{\code{"oscillation"}}{Sinusoidal burst (high HF energy ratio).}
\item{\code{"drift"}}{Monotonic linear trend (high trend slope, high Hurst).}
\item{\code{"sawtooth"}}{Repeating ramp pattern (high turning-point rate).}
}
}
}
\examples{
sim <- pheno_simulate(n = 600, seed = 42)
result <- suppressMessages(pheno_diagnose(sim$x, q = 2.5))
if (!is.null(result)) {
  ev <- pheno_evaluate(result, sim$true_labels)
  print(ev)
}

}
