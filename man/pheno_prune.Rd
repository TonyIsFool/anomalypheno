% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{pheno_prune}
\alias{pheno_prune}
\title{Merge Weak or Unstable Phenotypes}
\usage{
pheno_prune(result, min_instances = 2L, min_stability = 0.6, verbose = TRUE)
}
\arguments{
\item{result}{An \code{AnomPhenoResult} from \code{\link[=pheno_diagnose]{pheno_diagnose()}}.}

\item{min_instances}{Integer. Minimum number of instances for a phenotype
to be retained. Default: \code{2L}.}

\item{min_stability}{Numeric in [0,1]. Minimum mean stability score.
Default: \code{0.60}.}

\item{verbose}{Logical. Print merge summary. Default: \code{TRUE}.}
}
\value{
A pruned \code{AnomPhenoResult} with \verb{$pruned = TRUE} and
\verb{$n_merged} reporting how many phenotypes were absorbed.
}
\description{
Post-processes an \code{AnomPhenoResult} to \strong{merge over-split phenotypes} into
their nearest neighbours in the 27-dimensional standardised feature space.
A phenotype is considered "weak" if it has fewer than \code{min_instances}
members, or if its mean multi-resolution stability score is below
\code{min_stability}.

Weak phenotype instances are reassigned to the closest \emph{strong} phenotype
(by Euclidean centroid distance). The result is then re-characterised and
re-named using the discriminant-based naming algorithm.
}
\examples{
data(anomaly_benchmark)
result  <- suppressMessages(pheno_diagnose(anomaly_benchmark$x, q = 2.5))
if (!is.null(result) && result$mapper$n_phenotypes > 2) {
  pruned <- pheno_prune(result, min_instances = 2L, min_stability = 0.7)
  print(pruned)
}

}
