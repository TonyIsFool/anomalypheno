% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{pheno_score}
\alias{pheno_score}
\title{Compute a Continuous Anomaly Severity Score Series}
\usage{
pheno_score(
  result,
  type = c("combined", "zscore", "confidence"),
  normalise = FALSE
)
}
\arguments{
\item{result}{An \code{AnomPhenoResult} from \code{\link[=pheno_diagnose]{pheno_diagnose()}}.}

\item{type}{Character. Scoring mode: \code{"zscore"} (default), \code{"confidence"},
or \code{"combined"}.}

\item{normalise}{Logical. If \code{TRUE}, scale scores to [0, 1] by dividing
by the maximum observed score. Default: \code{FALSE}.}
}
\value{
Named numeric vector of length \code{length(result$x)}.
}
\description{
Returns a numeric vector of length \code{length(result$x)} where each anomaly
segment is assigned a continuous score and non-anomaly regions are zero.
Three scoring modes:

\describe{
\item{\code{"zscore"}}{Raw global z-score of each segment mean. Reflects how
far the anomaly is from the overall series distribution.}
\item{\code{"confidence"}}{Phenotype confidence score (0-1). Reflects how
unambiguously the instance belongs to its assigned phenotype.}
\item{\code{"combined"}}{Product of absolute z-score and confidence: a holistic
score that is large only when the anomaly is both severe and clearly
phenotyped. Recommended for monitoring dashboards.}
}

When anomaly segments overlap (rare with default \code{gap} settings), the
maximum score across overlapping assignments is used.
}
\examples{
data(anomaly_benchmark)
result <- suppressMessages(pheno_diagnose(anomaly_benchmark$x, q = 2.5))
if (!is.null(result)) {
  scores <- pheno_score(result, type = "combined", normalise = TRUE)
  plot(scores, type = "l", ylab = "Anomaly score", xlab = "Time index")
}

}
