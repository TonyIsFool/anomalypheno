% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{pheno_auto_tune}
\alias{pheno_auto_tune}
\title{Auto-Tune Detection Threshold via Grid Search}
\usage{
pheno_auto_tune(
  x,
  q_grid = c(1.5, 2, 2.5, 3, 3.5, 4),
  engine = c("sr", "lof"),
  criterion = c("composite", "silhouette", "ch"),
  min_instances = 3L,
  verbose = TRUE
)
}
\arguments{
\item{x}{Numeric vector. Time series.}

\item{q_grid}{Numeric vector. Candidate threshold values. Default:
\code{c(1.5, 2.0, 2.5, 3.0, 3.5, 4.0)}.}

\item{engine}{Character. Detection engine: \code{"sr"} or \code{"lof"}.}

\item{criterion}{Character. Optimisation criterion: \code{"silhouette"},
\code{"ch"} (Calinski-Harabász), or \code{"composite"} (average of all three).}

\item{min_instances}{Integer. Minimum anomaly instances required for a \code{q}
to be scored. Default: \code{3L}.}

\item{verbose}{Logical. Show progress. Default: \code{TRUE}.}
}
\value{
An \code{AnomPhenoTune} object with fields \code{best_q}, \code{best_result},
\code{metrics} (data frame), and \code{criterion}.
}
\description{
Searches a grid of \code{q} values, evaluates each using internal clustering
quality (Silhouette, CH, DB), and returns the configuration that best
separates phenotypes. Eliminates manual parameter selection.
}
\examples{
data(anomaly_benchmark)
tune <- suppressMessages(
  pheno_auto_tune(anomaly_benchmark$x,
                  q_grid = c(2.0, 2.5, 3.0), criterion = "composite"))
print(tune)

}
