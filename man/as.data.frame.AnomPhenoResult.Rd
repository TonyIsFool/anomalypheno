% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{as.data.frame.AnomPhenoResult}
\alias{as.data.frame.AnomPhenoResult}
\title{Coerce an AnomPhenoResult to a Tidy Data Frame}
\usage{
\method{as.data.frame}{AnomPhenoResult}(
  x,
  row.names = NULL,
  optional = FALSE,
  score_type = "combined",
  ...
)
}
\arguments{
\item{x}{An \code{AnomPhenoResult} from \code{\link[=pheno_diagnose]{pheno_diagnose()}}.}

\item{row.names}{\code{NULL} or a character vector of row names. Ignored.}

\item{optional}{Logical. Ignored (for S3 compatibility).}

\item{score_type}{Character. Score type passed to \code{pheno_score()}. One of
\code{"combined"} (default), \code{"zscore"}, \code{"confidence"}.}

\item{...}{Ignored (for S3 consistency).}
}
\value{
A \code{data.frame} with columns:
\describe{
\item{\code{time}}{Integer time index.}
\item{\code{value}}{Original series value.}
\item{\code{is_anomaly}}{Logical.}
\item{\code{anomaly_score}}{Continuous score (0 = normal).}
\item{\code{phenotype_id}}{Integer, \code{NA} for normal points.}
\item{\code{phenotype_name}}{Character, \code{NA} for normal points.}
\item{\code{phenotype_confidence}}{Numeric, \code{NA} for normal points.}
\item{\code{severity}}{Character, \code{NA} for normal points.}
}
}
\description{
Converts an \code{AnomPhenoResult} object into a \strong{tidy data frame} with one
row per time step, making results directly compatible with
\code{dplyr}, \code{ggplot2}, or CSV export workflows.
}
\examples{
data(anomaly_benchmark)
result <- suppressMessages(pheno_diagnose(anomaly_benchmark$x, q = 2.5))
if (!is.null(result)) {
  df <- as.data.frame(result)
  anomaly_rows <- df[df$is_anomaly, ]
  print(head(anomaly_rows))
}

}
