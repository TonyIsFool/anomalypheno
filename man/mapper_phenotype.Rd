% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{mapper_phenotype}
\alias{mapper_phenotype}
\title{Multi-Resolution Mapper Topological Phenotyping}
\usage{
mapper_phenotype(
  feat_mat,
  n_intervals = NULL,
  overlap = 0.5,
  n_clusters_max = 3L
)
}
\arguments{
\item{feat_mat}{Numeric matrix. Output of \code{\link[=extract_anomaly_features]{extract_anomaly_features()}}.}

\item{n_intervals}{Integer or \code{NULL}. Base cover intervals. If \code{NULL},
auto-selected via the Sturges rule: \code{ceiling(1 + log2(n))}. Default: NULL.}

\item{overlap}{Numeric. Interval overlap fraction. Default: 0.5.}

\item{n_clusters_max}{Integer. Max clusters per interval. Default: 3.}
}
\value{
A list of class \code{"AnomalyMapper"}:
\describe{
\item{\code{nodes}}{List of integer vectors (Mapper nodes).}
\item{\code{edges}}{2-column integer edge matrix.}
\item{\code{phenotype_id}}{Integer vector: phenotype per anomaly instance.}
\item{\code{n_phenotypes}}{Integer: number of distinct phenotypes.}
\item{\code{filter_vals}}{Numeric: PC1 filter values.}
\item{\code{stability}}{Numeric vector in [0, 1]: phenotype assignment
stability across the three resolution scales.}
}
}
\description{
An \strong{upgraded} implementation of the Mapper topological data analysis
algorithm (Singh et al., 2007) that addresses the classical parameter
sensitivity problem by running the algorithm simultaneously at \strong{three
resolution scales} and aggregating phenotype assignments via majority vote.
\subsection{Why Multi-Resolution?}{

A fundamental limitation of standard Mapper is that the phenotype structure
depends on the choice of \code{n_intervals}. This upgrade resolves the ambiguity:
three independent Mapper runs at n = floor(K), K, ceiling(1.5*K) (where K
is the Sturges-rule estimate) are combined via majority-vote phenotype
labelling. Assignments that are consistent across all three scales receive a
\strong{stability score} of 1.0; disagreements lower the score.
}
}
\references{
Singh, G., Mémoli, F., & Carlsson, G. (2007). Topological methods for the
analysis of high dimensional data sets and 3D object recognition.
\emph{Eurographics Symposium on Point-Based Graphics}, 91-100.
}
