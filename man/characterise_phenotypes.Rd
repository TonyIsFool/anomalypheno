% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{characterise_phenotypes}
\alias{characterise_phenotypes}
\title{Characterise Anomaly Phenotypes}
\usage{
characterise_phenotypes(x, anomaly_idx, feat_mat, mapper_res, seg_len = 10L)
}
\arguments{
\item{x}{Numeric vector. The original time series.}

\item{anomaly_idx}{Integer vector. Anomaly start indices.}

\item{feat_mat}{Numeric matrix. From \code{\link[=extract_anomaly_features]{extract_anomaly_features()}}.}

\item{mapper_res}{\code{AnomalyMapper} from \code{\link[=mapper_phenotype]{mapper_phenotype()}}.}

\item{seg_len}{Integer. Segment length used in feature extraction.}
}
\value{
A list of class \code{"AnomPhenoResult"} with components:
\describe{
\item{\code{phenotypes}}{\code{data.frame}: one row per phenotype.}
\item{\code{canonical_templates}}{Named list of numeric vectors (medoid segments).}
\item{\code{instance_table}}{\code{data.frame}: one row per anomaly instance,
including \code{phenotype_confidence} and \code{stability} columns.}
\item{\code{feature_importance}}{\code{data.frame}: features ranked by discriminability.}
\item{\code{mapper}}{The \code{AnomalyMapper} object.}
\item{\code{x}, \code{anomaly_idx}, \code{seg_len}}{Inputs for downstream functions.}
}
}
\description{
The final fitting stage of the AnomalyPheno pipeline. For each discovered
Mapper phenotype, computes:
\itemize{
\item A \strong{canonical template} (medoid: the instance with minimum total
distance to all other members in the phenotype).
\item A \strong{statistical summary} (amplitude, duration, frequency).
\item An \strong{automatic name} derived from dominant morphological and dynamic
features (e.g., \code{"Transient Elevation Spike"}, \code{"Sustained Depression Drift"}).
\item A \strong{severity class} (Mild / Moderate / Severe) from global z-scores.
\item A \strong{phenotype confidence score} for each instance: how unambiguously
does it belong to its assigned phenotype vs. the nearest alternative?
\item A \strong{feature importance table}: which of the 27 features best
discriminate between phenotypes (F-statistic-based ranking).
}
}
