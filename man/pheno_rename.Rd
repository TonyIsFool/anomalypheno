% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{pheno_rename}
\alias{pheno_rename}
\title{Rename Phenotypes Using Maximum Discriminant Feature Deviations}
\usage{
pheno_rename(result)
}
\arguments{
\item{result}{An \code{AnomPhenoResult} from \code{\link[=pheno_diagnose]{pheno_diagnose()}} or \code{pheno_prune()}.}
}
\value{
The same \code{AnomPhenoResult} with updated \verb{$phenotypes$name} column.
}
\description{
Replaces the phenotype names in an \code{AnomPhenoResult} with interpretable,
\strong{automatically-generated labels} derived from each phenotype's most
distinctive features relative to the global centroid.

Each phenotype is named by its top two deviating features:
\itemize{
\item \strong{Primary}: the feature where the phenotype centroid departs
most (in standardised units) from the cross-phenotype mean.
\item \strong{Secondary}: the next most distinctive feature.
}
}
\examples{
data(anomaly_benchmark)
result  <- suppressMessages(pheno_diagnose(anomaly_benchmark$x, q = 2.5))
if (!is.null(result)) {
  result2 <- pheno_rename(result)
  print(result2$phenotypes[, c("phenotype_id", "name")])
}

}
