% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{pheno_transition}
\alias{pheno_transition}
\title{Compute Phenotype-to-Phenotype Markov Transition Matrix}
\usage{
pheno_transition(result)
}
\arguments{
\item{result}{An \code{AnomPhenoResult} from \code{\link[=pheno_diagnose]{pheno_diagnose()}}.}
}
\value{
An \code{AnomPhenoTransition} object with:
\describe{
\item{\code{transition_matrix}}{K \eqn{\times} K row-stochastic probability matrix.}
\item{\code{transition_counts}}{Raw count matrix.}
\item{\code{stationary_dist}}{Stationary distribution vector (length K).}
\item{\code{sequence}}{Time-ordered phenotype ID sequence.}
}
}
\description{
Treats the temporally ordered sequence of anomaly phenotypes as a
discrete-time Markov chain and estimates the \strong{transition probability
matrix} P where P[i,j] = Prob(next anomaly is phenotype j | current
anomaly is phenotype i).

Also computes the \strong{stationary distribution} (long-run prevalence of each
phenotype) via power iteration, revealing which phenotype the system
gravitates towards over time.
}
\examples{
data(anomaly_benchmark)
result <- suppressMessages(pheno_diagnose(anomaly_benchmark$x, q = 2.5))
if (!is.null(result)) {
  tr <- pheno_transition(result)
  print(tr)
}

}
