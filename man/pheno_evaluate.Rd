% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluation.R
\name{pheno_evaluate}
\alias{pheno_evaluate}
\title{Evaluate Phenotype Recovery Against Ground-Truth Labels}
\usage{
pheno_evaluate(result, true_labels)
}
\arguments{
\item{result}{An \code{AnomPhenoResult} from \code{\link[=pheno_diagnose]{pheno_diagnose()}}.}

\item{true_labels}{Integer or character vector, \strong{same length as \code{result$x}}.
The ground-truth anomaly type for each time point. Normal points should
be labelled \code{0} or \code{"normal"}. If the vector is shorter and equals the
number of anomaly instances, it is treated as instance-level labels.}
}
\value{
A list of class \code{"AnomPhenoEval"} with:
\describe{
\item{\code{purity}}{Numeric in [0, 1].}
\item{\code{ari}}{Adjusted Rand Index (numeric).}
\item{\code{per_phenotype}}{\code{data.frame} with precision, recall, F1 per
phenotype.}
\item{\code{confusion}}{Confusion matrix (table).}
\item{\code{label_map}}{Named integer vector mapping predicted phenotype ID
to best-matching true label.}
}
}
\description{
When a reference labelling of anomaly types is available, \code{pheno_evaluate()}
quantifies how accurately the AnomalyPheno pipeline recovers the known
structure. Computes:

\describe{
\item{\strong{Purity}}{Fraction of instances whose predicted phenotype matches
the majority true label within that phenotype cluster.}
\item{\strong{Adjusted Rand Index (ARI)}}{Agreement between predicted and true
partitions, corrected for chance. ARI = 1 is perfect; ARI = 0 is
random; ARI < 0 is worse than random.}
\item{\strong{Per-phenotype precision / recall / F1}}{Computed after optimal
Hungarian-style label alignment (exhaustive permutation for <= 8
phenotypes; greedy for larger).}
\item{\strong{Phenotype confusion matrix}}{Full cross-tabulation of true vs.
predicted labels.}
}
}
\examples{
data(anomaly_benchmark)
result <- suppressMessages(pheno_diagnose(anomaly_benchmark$x, q = 2.5))
ev <- pheno_evaluate(result, anomaly_benchmark$true_labels)
print(ev)

}
