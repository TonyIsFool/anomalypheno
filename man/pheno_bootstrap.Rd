% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluation.R
\name{pheno_bootstrap}
\alias{pheno_bootstrap}
\title{Bootstrap Stability Analysis of Mapper Phenotyping}
\usage{
pheno_bootstrap(result, B = 200L, noise_sd = 0.05, seed = 42L, verbose = FALSE)
}
\arguments{
\item{result}{An \code{AnomPhenoResult} from \code{\link[=pheno_diagnose]{pheno_diagnose()}}.}

\item{B}{Integer. Number of bootstrap iterations. Default: 200.}

\item{noise_sd}{Numeric. Standard deviation of Gaussian noise added to
features in each bootstrap. Default: 0.05 (5\% of unit-normalised scale).}

\item{seed}{Integer. Random seed for reproducibility.}

\item{verbose}{Logical. Print progress. Default: \code{FALSE}.}
}
\value{
A list of class \code{"AnomPhenoBootstrap"} with:
\describe{
\item{\code{n_pheno_distribution}}{Table: observed counts of n_phenotypes.}
\item{\code{n_pheno_mode}}{Most frequent n_phenotypes across bootstrap runs.}
\item{\code{n_pheno_original}}{n_phenotypes from the original run.}
\item{\code{mean_jaccard}}{Mean pairwise Jaccard stability (numeric).}
\item{\code{phenotype_stability}}{Per-phenotype stability score (numeric vector).}
\item{\code{summary_df}}{\code{data.frame} for easy export.}
}
}
\description{
Assesses the robustness of phenotype discovery by running the Mapper
algorithm \code{B} times on bootstrap resamples (anomaly instances sampled
with replacement + small Gaussian noise on feature values). Reports:

\describe{
\item{\strong{n_phenotypes distribution}}{How often does the bootstrap recover
the same number of phenotypes as the original run?}
\item{\strong{Mean pairwise stability}}{Average Jaccard similarity between
phenotype memberships across bootstrap pairs.}
\item{\strong{Phenotype-level stability}}{For each phenotype in the original
run, the fraction of bootstrap samples that keep its core members
together.}
}

This function provides the empirical evidence that the discovered phenotype
structure is not a statistical artefact of the Mapper parameter choices --
a key requirement for a methods paper.
}
\examples{
\dontrun{
data(anomaly_benchmark)
result <- suppressMessages(pheno_diagnose(anomaly_benchmark$x, q = 2.5))
boot   <- pheno_bootstrap(result, B = 100, seed = 42)
print(boot)
}

}
