% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{pheno_fpca}
\alias{pheno_fpca}
\title{Functional Principal Component Analysis of Anomaly Waveforms}
\usage{
pheno_fpca(result, n_comp = 3L, align_length = NULL)
}
\arguments{
\item{result}{An \code{AnomPhenoResult} from \code{\link[=pheno_diagnose]{pheno_diagnose()}}.}

\item{n_comp}{Integer. Number of functional principal components. Default: \code{3L}.}

\item{align_length}{Integer. Common segment length after linear interpolation.
Default: \code{result$seg_len}.}
}
\value{
An \code{AnomPhenoFPCA} object.
}
\description{
Treats each detected anomaly segment as a \strong{functional object} and
decomposes the space of anomaly shapes via Functional Principal Component
Analysis (FPCA).

The k-th FPC captures the k-th dominant \strong{mode of shape variation} across
all anomaly instances. FPC scores (projections) may be used as additional
features for downstream clustering or regression.
}
\examples{
data(anomaly_benchmark)
result <- suppressMessages(pheno_diagnose(anomaly_benchmark$x, q = 2.5))
if (!is.null(result)) {
  fp <- pheno_fpca(result, n_comp = 3L)
  print(fp)
  plot(fp)
}

}
